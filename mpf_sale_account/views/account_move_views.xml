<?xml version="1.0" encoding="utf-8" ?>
<odoo>
  <record id="view_invoice_tree" model="ir.ui.view">
    <field name="model">account.move</field>
    <field name="inherit_id" ref="account.view_invoice_tree" />
    <field name="arch" type="xml">
      <field name="activity_ids" position="before">
        <field name="opportunity_id" optional="show" />
        <field name="lead_type_id" optional="show" />
        <field name="lead_brand_id" optional="show" />
      </field>
    </field>
  </record>

  <record id="view_move_form" model="ir.ui.view">
    <field name="model">account.move</field>
    <field name="inherit_id" ref="account.view_move_form" />
    <field name="arch" type="xml">
      <xpath expr="//div[@name='journal_div']" position="after">
        <field
          name="opportunity_id"
          invisible="move_type not in ('out_invoice', 'out_refund')"
        />
        <field
          name="lead_type_id"
          invisible="move_type not in ('out_invoice', 'out_refund')"
          readonly="1"
        />
        <field
          name="lead_brand_id"
          invisible="move_type not in ('out_invoice', 'out_refund')"
          readonly="1"
        />
      </xpath>
    </field>
  </record>

  <record id="view_account_invoice_filter" model="ir.ui.view">
    <field name="model">account.move</field>
    <field name="inherit_id" ref="account.view_account_invoice_filter" />
    <field name="arch" type="xml">
      <field name="date" position="after">
        <field name="opportunity_id" />
        <field name="lead_type_id" />
        <field name="lead_brand_id" />
      </field>
      <filter name="salesperson" position="before">
        <filter
          string="Type"
          name="group-lead_type_id"
          context="{'group_by':'lead_type_id'}"
        />
        <filter
          string="Brand"
          name="group-lead_brand_id"
          context="{'group_by':'lead_brand_id'}"
        />
      </filter>
    </field>
  </record>
</odoo>
